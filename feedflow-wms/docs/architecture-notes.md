# 아키텍처 노트 — 다중 창고(Center) 확장

WMS 1.0 은 **단일 물류센터 안에 창고 건물 2동(`WH1` · `WH2`)** 이 있는 구조를 전제로 만들었다.
에픽 "전국 다중 창고 기반 물류망 확장" 에서 손대야 할 지점을 정리한 문서다.

## 에픽 진행 상태

| Phase | 내용 | 상태 |
|---|---|---|
| **P1 기초 공사** | `Warehouse` enum → `Center` 엔티티, 구역(Bin) 매핑 전환 | ✅ 완료 |
| **P2 검색 및 조회** | 재고 · 이력 조회에 센터 필터 축 추가 | ✅ 완료 |
| **P3a 센터 간 이동** | `TRANSFER_OUT` / `TRANSFER_IN`, `IN_TRANSIT` 가상 구역 | ✅ 완료 |
| P3b 운송 중 상태 관리 | 도착 처리 분리 · 이관 전표 | ⏸ 보류 (포트폴리오 범위 초과) |
| **P4a 대시보드** | 센터별 재고 분포 · 실적 시각화 | ✅ 완료 |
| **P4c 전국 거점 지도** | `Center` 좌표 컬럼 + Leaflet 지도 · 2D 도면 양방향 연동 | ✅ 완료 |
| P4b 스마트 주문 할당 | 배송지 → 센터 라우팅 | ⏸ 보류 (배송지 좌표 · 권역 우선순위 부재) |

## 창고를 표현하는 방식 (P1 이후)

| 요소 | 현재 | 위치 |
|---|---|---|
| 센터 구분 | `centers` **테이블** (운영 중 추가 · 중지 가능) | `domain/Center.java` |
| 구역의 소속 | `WarehouseBin.center` — `@ManyToOne(LAZY, optional = false)` | `domain/WarehouseBin.java` |
| 재고의 소속 | **없음.** `Inventory` 는 구역(`bin`)만 참조하고 센터를 직접 모른다 | `domain/Inventory.java` |

`Inventory` 가 센터를 직접 모르는 것은 정규화 관점에서 맞다. 센터는 구역의 속성이고,
재고는 구역에 속하므로 `inventory → bin → center` 로 도달할 수 있다.
다만 **센터 단위로 재고를 필터링하는 쿼리가 아직 없다** (P2에서 추가).

## P1 에서 실제로 바꾼 것

| 대상 | 변경 |
|---|---|
| `Warehouse` enum | **파일 삭제.** enum 을 남겨두면 센터 정보가 두 곳에서 관리된다 |
| `Center` 엔티티 신설 | `centerId` · `centerCode` · `name` · `region` · `address` · `note` · `active` · `createdAt` |
| `WarehouseBin.warehouse` | `@ManyToOne Center center` (`centerId` FK, `optional = false`) |
| `WarehouseBinForm.warehouse` | `centerId` — select 옵션을 enum 상수 대신 `CenterRepository` 조회로 |
| 모든 구역 조회 쿼리 | `join fetch b.center` 추가 — `locationLabel()` 이 센터명을 쓰므로 없으면 N+1 |
| `WarehouseMapRow` | 센터 컴포넌트 제거. 도면은 센터 단위로 그리므로 행마다 담을 필요가 없다 |
| `WarehouseFacilityDto` | `forWarehouse(Warehouse)` → `forCenter(Long)`. `switch` 제거 (WH1/WH2 배치가 완전히 동일했다) |
| `data.sql` | `centers` 시드 2행 추가, `warehouseBins.warehouse` → `centerId` (40행) |
| `docs/erd-*.tsv` · `erd.md` | `centers` 테이블 · 관계 반영 |

**P1 에서는 센터의 의미를 바꾸지 않았다.** `centerCode` 는 `WH1`/`WH2`, `name` 은
`제1창고`/`제2창고` 그대로 두었다. 구조 전환과 데이터 의미 변경을 한 커밋에 섞으면
화면 결과가 달라져 **회귀인지 의도된 변경인지 구분할 수 없다.**

> 전국 센터 코드 체계(`C1-YS` ~ `C5-NJ`)와 실제 거점 5곳으로의 교체는 **P4a 에서 했다.**
> 그때는 이미 P2 · P3a 테스트가 센터 축을 검증하고 있어, 시드를 갈아도 깨지는 지점이
> 바로 드러났다.

## 남은 영향 지점

### 1. 재고 조회의 센터 축 (P2 완료)

| 위치 | 현재 시그니처 | 센터 축 |
|---|---|---|
| `InventoryRepository.search` | `(centerId, productId, binId, zone)` | ✅ |
| `InventoryService.getInventories` | `(centerId, productId, binId, zone)` | ✅ |
| `InventoryRepository.findStockByCenter` | `(productId)` → 센터별 집계 | ✅ 신규 |
| `InventoryService.getDisposalTargets` | `(productId, zone, expiredOnly)` | ⏳ 미적용 |
| `InventoryMoveService.getMovableInventories` | `(binId)` | 불필요 (구역이 센터를 결정한다) |

폐기 화면은 만료 재고를 **전국 단위로 훑는 것이 기본 동작**이라 센터 조건을 넣지 않았다.
요구가 나오면 `:centerId` 를 추가한다.

> **이 파라미터들을 조건 객체로 묶지 않았다.**
> Spring Data JPA 의 `@Query` 는 `@Param` 으로 개별 인자를 받으므로, 서비스에서 객체로 묶어도
> Repository 경계에서 다시 풀린다. 변환 코드만 늘고 이득이 없다.
> `search` 가 인자 4개가 되면서 경계에 다가섰다. **다음에 축이 하나 더 붙으면 묶는다.**

### 1-1. 정렬 우선순위는 유통기한이다 (P2 결정)

재고 현황 목록을 센터 순으로 먼저 정렬하지 않았다. 이 화면은 **FEFO 출고 순서를 눈으로
확인하는 용도**이므로, 센터를 1순위로 두면 가장 급한 재고가 화면 아래로 밀린다.
유통기한이 같을 때만 `centerCode → binCode` 로 묶어 같은 센터 행이 흩어지지 않게 했다.

### 1-2. 전국 기준 지표와 필터된 목록을 구분해야 한다 (P2 결정)

요약 카드(적재 위치 수 · 총 보관 수량)는 **전국 기준을 유지**했다. 필터에 따라 카드까지
바뀌면 "전국에 얼마나 있는지" 를 볼 방법이 사라진다. 대신 두 가지를 추가했다.

- 카드에 `전국` 뱃지 — 기준을 명시
- `InventorySearchDto` — 지금 보고 있는 목록의 합계를 함께 내려 목록 헤더에 표시

센터별 분포(`findStockByCenter`)도 **목록 필터와 무관하게** 집계한다. 목록을 자바에서
그룹핑하면 센터를 하나 고른 순간 분포도 그 센터 하나로 줄어들어, 정작 필요한
"다른 센터에도 재고가 있다" 는 정보가 사라진다.

### 2. 센터별로 달라져야 하는 전역 상수

| 대상 | 현재 | 확장 시 |
|---|---|---|
| 도면 격자 크기 | `WarehouseBinForm.GRID_COLUMNS = 26` · `GRID_ROWS = 14` (전역) | 센터마다 건물 크기가 달라 **센터별 속성**이어야 한다. Bean Validation `@Max` 가 컴파일 상수를 요구하므로 폼 검증 방식도 함께 바꿔야 한다 |
| 부대시설(출입구·벽·검수실) | `WarehouseFacilityDto.forCenter(Long)` — 모든 센터가 같은 배치 | 센터별 배치가 달라지면 `centerFacilities` 테이블로 이관. P1 에서 `switch` 를 걷어냈으니 이관 지점이 한 곳으로 좁혀졌다 |

### 3. 센터 간 재고 이관 (P3a 완료)

`MOVE` 로 센터를 넘을 수 있었던 문제를 해결했다. 이제 센터가 다르면
`TRANSFER_OUT` + `TRANSFER_IN` 두 건으로 기록되고, 그 사이 재고는
**출발 센터 소속 `IN_TRANSIT` 가상 구역**을 경유한다.

| 구분 | 이력 | sign |
|---|---|---|
| 같은 센터 | `MOVE` 1건 | 0 |
| 다른 센터 | `TRANSFER_OUT` + `TRANSFER_IN` 2건 | -1 / +1 (합 0) |

**운송 중 재고를 `Inventory` 밖에 두지 않은 이유** — 3계층 불변식이 정합성 점검의
근거인데, 밖에 두면 운송 중에 불변식이 깨져 점검 로직에 예외를 넣어야 한다.
그 예외가 진짜 어긋남을 가려버린다. 안전 장치를 위해 안전 장치를 무디게 하는
거래는 하지 않았다. 상세는 [epic-p3-design.md](epic-p3-design.md).

**가상 구역이 새어 나가지 않도록 막은 곳** — 2D 도면 / 구역 선택 목록 /
등록·수정 폼의 용도 select / 적재 한도 검증 / 적재율 통계.
대신 **정합성 점검 화면에서는 잔류 재고를 보여준다.** 감추기만 하면
이관이 중간에 깨져도 알아챌 방법이 없다.

**P3b 확장 지점** — 두 구간이 이미 분리되어 있으므로, 실제 운송 중 상태가
필요해지면 `recordTransfer` 의 두 번째 구간을 즉시 호출하지 않으면 된다.
구조를 다시 짜지 않는다.

> P1 · P2 에서 `InventoryMoveService` 의 이동 로직은 손대지 않았다. 구역 간 이동의 판정
> 단위는 **구역**이고, 구역이 어느 센터에 속하는지는 이동 로직의 관심사가 아니다.
>
> **다만 이동 화면은 여전히 다른 센터의 구역을 고를 수 있다.** 막으려면 `TRANSFER_*` 유형과
> 운송 중 상태가 함께 있어야 하므로 P3 에서 한 번에 처리한다.
> P2 에서는 그런 이력이 생겼을 때 **조용히 묻히지 않도록** 감지 장치만 넣었다 —
> `TraceEventDto.isCenterTransfer()` 가 출발·도착 센터가 다른 `MOVE` 를 판정하고,
> 타임라인과 로트 요약이 경고로 표시한다.

### 4. 전국 대시보드 (P4a 완료)

센터별 재고 분포 · 적재율 · 유통기한 경보 · 기간 실적을 대시보드에 올렸다.
`CenterDashboardService` 를 `DashboardService` 와 <b>분리</b>했다 — 전자는 센터별로
쪼개서 비교하고, 후자는 전국 합계를 요약한다. 두 관점을 한 서비스에 섞으면
메서드마다 "이건 전국인가 센터별인가" 를 확인해야 한다.

**조립 쿼리 6회로 고정** — 센터 목록 · 재고 · 수용량 · 경보 · 실적 · 축종 구성을
각각 한 번씩 집계해 센터 단위로 짝짓는다. 센터마다 반복 조회하면 센터 수만큼 늘어난다.

**센터 목록을 기준으로 조립한다.** 집계 쿼리는 `group by` 결과라 재고가 있는 센터만
돌려준다. 집계 결과를 기준으로 조립하면 <b>재고 0 인 센터가 화면에서 사라져</b>
신설 센터에 배분이 누락된 사실을 아무도 알아채지 못한다.

**이관 합계는 출고 기준으로만 센다.** 출고와 입고는 짝이므로 더하면 같은 물량이
두 번 잡힌다. 60포대 이관이 120으로 보인다.

**적재율 경계는 `BinLoadStatus` 상수를 재사용한다.** 여기서 60 / 90 을 다시 적으면
한쪽만 고쳐졌을 때 같은 센터가 2D 도면에서는 '보통', 대시보드에서는 '포화' 로 보인다.

### 5. 전국 거점 지도 (P4c 완료)

2D 도면은 **창고 한 동의 내부**를 보여준다. 전국 5거점으로 확장했어도 도면만으로는
"이 창고가 전국에서 어디인지" 가 드러나지 않아, 탭 이름을 읽어야 위치를 알 수 있었다.
`/admin/warehouse-map` 에 실제 지도를 올려 두 축을 함께 본다.

#### 좌표를 `Center` 컬럼으로 둔 이유 (프론트엔드 Geocoder 거부)

| 방식 | 판단 |
|---|---|
| **`Center.latitude` · `longitude` 컬럼** | 채택. 좌표는 **기준 정보**다 |
| 화면에서 `address` → 좌표 변환 (Geocoder) | 거부 |

- 좌표는 파생값이 아니라 센터가 가진 속성이다. 주소를 바꾸지 않는 한 변하지 않는다.
- 변환 API 는 키와 호출 한도가 있고, **실패하면 지도에 핀이 하나도 찍히지 않는다.**
  화면을 열 때마다 외부 서비스에 성패를 맡길 이유가 없다.
- 기획 단계 주소는 `"고덕면 몽곡리 667 일대"` 처럼 **범위**로 적혀 있어 변환 결과가
  호출 시점마다 달라질 수 있다. 핀이 조금씩 움직이는 지도가 된다.

#### 좌표를 nullable 로 둔 이유

부지를 확정하기 전에 센터를 먼저 만들어 재고를 배분하는 일이 실제로 생긴다.
좌표가 없으면 **지도에서만 빠지고** 도면 · 재고 · 이관은 그대로 동작한다.

대신 응답에 `missingCount` 를 함께 담아 **몇 곳이 빠졌는지 화면에 알린다.**
조용히 빼면 핀 수가 센터 수와 달라도 아무도 눈치채지 못한다.
위도만 있는 센터도 누락으로 센다 — 위도만 아는 좌표는 좌표가 아니다.

#### 지도 라이브러리 = Leaflet + OpenStreetMap

카카오맵 · 네이버지도를 쓰지 않았다. **앱 키 발급과 도메인 등록 없이는 지도가 아예
뜨지 않기 때문**이다. 포트폴리오를 받아 실행하는 사람이 키를 설정하지 않으면 빈 화면을
보게 된다. Leaflet(BSD-2) + OSM 타일은 키 없이 바로 뜨고 마커 · 팝업 · 이동
애니메이션이 모두 있어 양방향 연동에 필요한 것이 다 있다.

교체 지점을 `createMap` / `addPin` / `focusPin` 세 함수로 국소화하고 주석에 적어 두었다.
카카오맵으로 바꿀 때 고칠 곳이 세 군데뿐이다.

#### 양방향 연동은 방향에 따라 처리가 다르다

| 방향 | 처리 | 이유 |
|---|---|---|
| 지도 핀 → 도면 | **페이지 이동** (`?centerId=`) | 도면은 서버 렌더링이다. 구역 좌표 · 적재율 집계를 모두 서버가 계산하므로 센터가 바뀌면 다시 받아야 한다 |
| 도면 탭 → 지도 | **같은 센터면 `preventDefault()` 후 지도만 이동** | 클라이언트에 이미 모든 핀이 있다. 매번 페이지를 다시 받으면 지도가 처음부터 로드되며 깜빡인다 |
| 탭 hover | 핀만 강조 (이동 없음) | 마우스가 지나가기만 해도 지도가 움직이면 어지럽다 |

지도는 **보조 정보**이므로 별도 `fetch` 로 가져오고 모든 실패를 잡아 안내 문구로만
표시한다. CDN 이나 타일 서버가 죽어도 2D 도면 본문은 그대로 보여야 한다.

> **CSS 두 줄이 실패 원인이 되기 쉽다.**
> `height` 를 지정하지 않으면 Leaflet 이 타일을 그리지 않는데 콘솔에 오류가 없어
> 원인을 찾기 어렵다. `z-index: 0` 은 지도 컨트롤이 사이드바(1040/1045) 위로
> 올라오는 것을 막는다. 둘 다 `admin.css` 에 주석과 함께 명시했다.

### 5-1. 스마트 주문 할당을 여전히 보류하는 이유 (P4b)

P4c 로 **센터 좌표는 확보했다.** 그래도 라우팅에는 아직 두 가지가 없다.

| 필요 | 현재 |
|---|---|
| ~~센터 좌표~~ | ✅ `Center.latitude` · `longitude` (P4c) |
| 주문 배송지 좌표 | `Order.shippingAddress` 문자열뿐. 지오코딩 API 가 없으면 주소 파싱이 된다 |
| 권역 → 센터 우선순위 | 없다. 거리만으로 정할 수 없다 (센터별 취급 축종이 다르다) |

주소 문자열 파싱은 규칙이 지저분하고(`경북 상주시` / `경상북도 상주시` / `상주시`)
예외가 끝없이 나온다. 그러면 <b>보여주려는 것(전국 물류망 설계)이 아니라
주소 처리 코드에 시간을 쓰게 된다.</b>

또한 최단 거리 센터에 배정하면 **그 센터가 해당 축종을 취급하지 않을 수 있다.**
나주는 가금 전용이라 소 사료 주문을 받을 수 없다. 거리 + 취급 축종 + 가용 재고를
함께 보는 규칙이 필요하고, 그건 좌표가 있다고 해결되지 않는다.

## 시드 데이터는 '정합성' 과 '보여줄 수 있는지' 를 따로 본다

`data.sql` 은 4가지 정합성 규칙을 만족한다(요구사항 문서 참조). 그런데 규칙을 다 지켜도
**기능을 보여주지 못하는 데이터**가 만들어진다. P4a 직후 실제로 그랬다.

| 증상 | 규칙 위반? | 화면에서 보이는 것 |
|---|---|---|
| 최근 7일 입고 +1,660 / 출고 -10 | 아니다 | "물류가 멈춘 고인 물 창고" |
| 활성 보관 구역인데 재고 0 | 아니다 | 2D 도면에 영구히 빈 칸 |
| 시드 상태에서 이미 포화(80%↑) | 아니다 | 재고 이동을 시연할 여유가 없다 |
| 가금 전용 센터에 저온(영양제) 구역 | 아니다 | 절대 채워지지 않는 구역 |

그래서 검증에 **표시 품질** 항목을 따로 두었다. 정합성만 보는 검증은
"입고 0건" 같은 문제를 통과시킨다.

### 출고 이력 보강 방식

`SHIPPED` · `DELIVERED` 주문 10건 전부에 `OUTBOUND` 를 붙였다. 배송 완료인데 창고에서
나간 기록이 없는 주문은 그 자체로 이상하다.

문제는 출고를 더하면 최종 재고가 줄어든다는 것이다. 그래서 **짝이 되는 `INBOUND` 를
25일 더 오래된 날짜로 함께 넣었다.**

```
입고 +q (25일 이상 과거)  +  출고 -q (주문일)  =  0
```

기간 집계(최근 7일)는 출고가 움직이고, 최종 재고 배치는 그대로다.
결과는 최근 7일 입고 **+310** / 출고 **-180** · 폐기 -15 다.

### 로트 배분은 적재율이 낮은 구역 순으로 나눈다

한 구역에 다 들어가는 로트도 여러 구역에 나눠 담는다. 앞에서부터 채우면(greedy)
앞쪽 구역은 100%, 뒤쪽은 0% 가 되어 **포화 6곳 · 빈칸 6곳**이 동시에 생겼다.
"잔여 용량이 많은 순" 도 수용량이 다른 구역 사이에서는 균형이 맞지 않아,
**적재율이 낮은 순**으로 골랐다.

분할 최소 단위는 100포대다. 70 은 재고 행이 69개까지 늘어나며 과분할되고,
130 은 다시 빈 칸이 생겼다.

### 이력만으로 현재 재고를 재현할 수 있게 맞췄다

이력 누적 = 최종 재고를 **로트 단위뿐 아니 `로트 × 구역` 단위까지** 맞췄다.
`MOVE` · `TRANSFER_OUT` 의 출발 구역에 그 로트가 입고된 기록이 없으면,
로트 총량은 맞아도 **구역별로는 이력과 재고가 어긋난다.** 이력 추적 화면이
"이력상 수량과 실제 재고가 다르다" 고 경고하는 조건이 바로 그것이다.

## 재고가 있다고 다 내보낼 수 있는 것은 아니다

`findAllocatableByProductId()`(FEFO 출고 후보)가 **구역 용도를 전혀 걸러내지 않고 있었다.**
유통기한과 사용 중지 구역만 제외했으므로, 입고 검수 전 재고와 **운송 중(트럭 위) 재고까지
출고 후보에 들어갔다.**

시드에 대기 구역 재고가 0이라 아무도 눈치채지 못했다. 그런데 화면의 `입고 등록` 은
입고 대기 구역을 고를 수 있으므로, 담당자가 한 번 그렇게 넣으면 **검수도 하지 않은 물건이
고객에게 나갈 수 있었다.**

| 용도 | 출고 가능 | 이유 |
|---|---|---|
| `STORAGE` 보관 | ✅ | |
| `SHIPPING` 출고 대기 | ✅ | 이미 피킹해 모아 둔 물량이다 |
| `RECEIVING` 입고 대기 | ❌ | 검수를 통과하지 않았다. `구역 간 이동` 으로 보관 구역에 넣어야 가용 재고가 된다 |
| `INSPECTION` 검수 | ❌ | 검수 중이다 |
| `IN_TRANSIT` 운송 중 | ❌ | 트럭 위에 있다. 집어올 수 있는 물건이 아니다 |

`BinPurpose` 는 이미 세 축(`countedInCapacity` · `physicalSpace` · `systemManaged`)으로
성격을 나누고 있었는데 **"출고 가능한가" 축이 없었다.** 다만 JPQL 이 열거형 메서드를
호출할 수 없으므로, 진실이 두 곳으로 갈라지지 않게 **조건은 쿼리에만 두고** 판단 근거는
주석으로 남겼다.

### 그래서 `totalStock` 과 출고 가능 수량은 다를 수 있다

시드의 품목 9(육계 후기 사료)가 그 예다 — **전체 720 / 출고 가능 600.** 나머지 120은
예산 센터 입고 대기 구역에서 검수를 기다린다. 이 상태는 오류가 아니라 정상이다.

문제는 담당자가 *"재고가 720인데 왜 부족하다는 거냐"* 고 되묻게 된다는 것이었다.
제외 사유를 안내하는 곳이 **여섯 군데**나 있었고 전부 **유통기한과 사용 중지 구역만**
말하고 있었다 — 지금 막힌 실제 이유(입고 대기 구역)는 어디에도 없었다.
없는 만료 로트를 찾게 만든다.

| 위치 | 성격 |
|---|---|
| `InsufficientStockException` 메시지 | 출고 실패 시 |
| `outbound-preview.js` 부족 배너 | 실시간 미리보기 (화면에서 제일 먼저 보인다) |
| `direct.html` 품목 선택 도움말 | 전체 재고와 다를 수 있다는 예고 |
| `direct.html` FEFO 규칙 설명 | 규칙 목록 |
| `OutboundService` · `OutboundApiController` javadoc | 코드 읽는 사람용 |

여섯 곳 모두 네 가지 사유를 말하도록 맞췄다.

### 입고 직후에 알려주는 것이 가장 중요하다

출고가 막힌 시점에 설명하는 것은 늦다. 그래서 **입고 결과 화면**에 경고를 넣었다
(`InboundResultDto.isNotShippableBin()`). 만료 로트 경고와 이유는 다르지만 결과는 같다 —
**재고 수량은 늘어나는데 출고 가능 재고는 늘어나지 않는다.** 그 자리에서
`구역 간 이동` 링크를 걸어 바로 해결할 수 있게 했다.

### 이 조건은 JPQL 안에만 있다

서비스 단위 테스트는 리포지토리를 목으로 대체하므로 이 조건을 검증할 수 없다.
`AllocatableStockRepositoryTest`(`@DataJpaTest`)로 실제 H2 에 다섯 용도의 구역을 만들어
보관·출고 대기만 후보로 나오는지 확인한다. 미리보기용 일괄 조회
(`findAllocatableByProductIds`)와 조건이 같은지도 함께 고정했다 — 한쪽만 고치면
미리보기에서는 보이던 재고가 실제 출고 때 사라진다.

## 적재율의 분자와 분모는 같은 구역 집합을 세야 한다

P4a 의 센터 카드와 P4c 의 지도 팝업이 적재율을 이렇게 계산하고 있었다.

```
적재율 = 센터의 전체 재고 / 보관 구역 수용량       ← 분자와 분모의 기준이 다르다
```

분모는 `findStorageCapacityByCenter()` 로 **활성 보관 구역만** 센다. 그런데 분자는
`findStockByCenter()` 로 **모든 구역**을 셌다 — 입고 대기, 출고 대기, 운송 중,
사용 중지 구역까지. 시드에는 대기 구역 재고가 없어 두 값이 우연히 같았고,
그래서 화면상 아무 문제도 드러나지 않았다.

**입고 등록으로 대기 구역에 물건이 들어오는 순간 어긋난다.** 같은 센터가
2D 도면에서는 60%, 대시보드에서는 90% 로 보인다. 도면은 대기분을 적재율에서 빼고
`+ 대기 구역 N포대 별도` 로 따로 표시하기 때문이다. 심하면 적재율이 100% 를 넘는다.

`WarehouseBinRepository.findStorageCapacityByCenter()` 의 주석에 이미
*"기준이 도면과 다르면 같은 센터의 적재율이 화면마다 다르게 보인다"* 고 적어 두었는데,
분모만 그 기준을 지키고 분자는 지키지 않았다.

### 두 값을 나눠서 각자의 쓰임에 맞게 쓴다

| 값 | 쿼리 | 쓰임 |
|---|---|---|
| 전체 재고 | `findStockByCenter()` | **분포 · 비중** — 대기 구역 물건도 그 센터에 실물로 있고, 운송 중 재고도 출발 센터로 잡아야 전국 합계가 맞는다 |
| 보관 구역 재고 | `findStorageStockByCenter()` | **적재율 · 남은 여유 · 적재 상태 뱃지** — 분모와 기준이 같다 |

차이(`전체 − 보관`)는 숨기지 않고 `waitingQuantity` 로 화면에 표시한다.
그러지 않으면 "재고 900 인데 적재율은 60%" 를 설명할 수 없다.
2D 도면이 이미 쓰던 방식과 같다.

## 유지한 불변식

| 항목 | 확장 시 이점 |
|---|---|
| `BinCapacityChecker` | 판정 단위가 **구역(bin)** 이라 구역이 어느 센터에 속하든 코드가 바뀌지 않는다. 적재 한도는 센터가 아니라 구역의 속성이다 |
| `StockPolicy` 상수 통합 | 유통기한 임박 기준이 한곳에 모여 있다. 센터별 정책이 필요해지면 이 클래스만 설정 주입으로 바꾸면 된다 |
| 재고 3계층 | `Product.totalStock` = Σ`ProductLot.lotQuantity` = Σ`Inventory.quantity`. 센터가 늘어도 성립한다 |
| `zoneLabel()` / `locationLabel()` 분리 | 센터를 별도 컬럼으로 보여주는 화면과 그렇지 않은 화면이 같은 라벨을 쓰면 센터명이 한 행에 두 번 나온다. 표기 책임을 화면이 고르게 했다 |
| 집계 record → 화면 DTO 변환 규약 | `StockSyncRow → StockSyncResultDto`, `DailySalesRow → SalesChartDto`, `CenterStockRow → CenterStockDto`. 비중(%) 처럼 **다른 행과의 관계에서 나오는 값**은 행 하나만 아는 projection 이 계산할 수 없다 |

## `Product.totalStock` 은 전국 합계로 유지한다 (확정)

센터별로 쪼개지 않는다. **B2C 쇼핑몰이 이 컬럼을 "판매 가능 수량" 으로 읽고 있어
의미를 바꾸면 연동이 깨진다.** 재고 정합성 점검(`/admin/inventory/sync`)도 전국 합계 기준을 유지한다.

센터별 가용 재고가 필요하면 `inventories` 를 `bin → center` 로 조인해 집계 쿼리를 따로 만든다.
비정규화 캐시를 하나 더 두는 것(`centerStock`)은 갱신 지점이 늘어 정합성 위험만 커진다.
