# FeedFlow 요구사항 정의서

배합사료 유통 관리 플랫폼의 **관리자 시스템(Admin) 및 대시보드 모듈** 요구사항과 구현 현황.

- **개발 범위**: 관리자/창고(WMS) 기능만. B2C 농가용 쇼핑몰 기능은 이 저장소의 범위가 아니다.
- **기술 스택**: Java 21 / Spring Boot 3.5.16 / Gradle / H2(in-memory) / Spring Data JPA / Thymeleaf / Bootstrap 5 / Chart.js
- **DB 명명 규칙**: 테이블·컬럼 모두 camelCase (`.kiro/steering/db-naming-convention.md` 참고)
- **ERD**: [docs/erd.md](erd.md) (Mermaid), `docs/erd-columns.tsv` · `erd-relations.tsv` · `erd-enums.tsv`

## 표기 규칙

| 표기 | 뜻 |
|---|---|
| 완료 | 기능이 구현되어 화면/API로 동작하며 단위 테스트가 있다 |
| 부분 | 도메인·서비스는 있으나 화면 또는 실행 경로가 없다 |
| 미착수 | 코드가 존재하지 않는다 |

커밋 해시는 해당 기능이 **처음 도입된** 커밋이다. 이후 수정·개선 커밋은 별도로 적지 않았다.

> 로컬 저장소는 shallow clone(depth=1) 상태라 `001d65d` 이전 이력이 없다. 초기 구축 단계에서
> 한꺼번에 들어온 기능들은 모두 `001d65d`(128개 파일)로 표기한다. 정확한 최초 커밋이 필요하면
> GitHub 저장소에서 전체 이력을 확인해야 한다.

## Must Have (필수)

| # | 기능 | 상태 | 커밋 | 주요 경로 |
|---|---|---|---|---|
| M1 | 관리자 로그인 / 권한 체계 (`ADMIN` · `STAFF` · `USER`) | 완료 | `001d65d` | `/login`, Spring Security, `LoginUser` |
| M2 | 대시보드 — 권한별 뷰 분리 | 완료 | `001d65d` | `/admin/dashboard` |
| M2-1 | └ 안전재고 미달 알림 (STAFF·ADMIN) | 완료 | `001d65d` | `DashboardService` |
| M2-2 | └ 유통기한 임박 알림 (30일 기준, STAFF·ADMIN) | 완료 | `001d65d` | `feedflow.dashboard.expiration-alert-days` |
| M2-3 | └ 오늘의 할 일 — 신규 주문 · 출고 대기 건수 | 완료 | `001d65d` | `DashboardService` |
| M2-4 | └ 매출 통계 요약 (ADMIN 전용) | 완료 | `001d65d` | `sec:authorize="hasRole('ADMIN')"` |
| M2-5 | └ 최근 7일 매출 추이 차트 (ADMIN 전용, 비동기) | 완료 | `001d65d` | `AdminRestController`, Chart.js |
| M3 | 사원 계정 · 권한 변경 (ADMIN 전용) | 완료 | `001d65d` | `/admin/employees` |
| M4 | 품목 관리 (등록 · 수정 · 사용중지) | 완료 | `001d65d` | `/admin/products` |
| M4-1 | └ 축종 3종(소·돼지·조류) · 품목구분(사료·영양제) enum 전환 | 완료 | `6343f60` | `AnimalType`, `ProductType` |
| M5 | 창고 구역(Bin) 관리 | 완료 | `001d65d` | `/admin/bins` |
| M6 | 재고 현황 조회 (품목 · 구역 · 존 필터) | 완료 | `001d65d` | `/admin/inventory` |
| M7 | **입고 등록** — 로트 생성/합산, 유통기한 자동 계산, 구역 한도 검증 | 완료 | `001d65d` | `/admin/inventory/inbound`, `InventoryService.receive()` |
| M8 | 재고 폐기 (ADMIN 전용, 사유 필수) | 완료 | `001d65d` | `/admin/inventory/disposal` |
| M9 | 입출고 이력 조회 (유형 · 품목 필터, 페이징) | 완료 | `001d65d` | `/admin/inventory/movements` |
| M10 | FEFO(선입선출) 주문 출고 — 할당 미리보기 → 처리 | 완료 | `001d65d` | `/admin/outbound`, `OutboundService` |
| M10-1 | └ 주문 목록 상태 필터 (출고대기 · 출고완료 · 배송완료 · 취소 · 전체) | 완료 | `e83f8ac` | `OrderListFilter` |
| M11 | 직접 출고 (주문과 무관한 출고) | 완료 | `001d65d` | `/admin/outbound/direct` |
| M12 | 출고(주문) 취소 + 재고 원상 복구 (ADMIN 전용) | 완료 | `f47db83` | `OrderCancellationService` |
| M12-1 | └ 취소 사유 · 일시 · 처리자 기록 및 화면 표시 | 완료 | `e058e22` | `Order.cancel(reason, userId, userName)` |
| M13 | 바코드/QR 스캔 입출고 | 완료 | `001d65d` | `/admin/scan` |
| M14 | QR 라벨 출력 | 완료 | `001d65d` | `/admin/scan/labels` |
| M15 | 재고 정합성 점검 · 보정 (진단 STAFF·ADMIN / 보정 ADMIN) | 완료 | `79d69ef` | `/admin/inventory/sync` |
| M16 | 낙관적 락(`@Version`) 동시성 제어 | 완료 | `001d65d` | `Product` · `ProductLot` · `Inventory` |
| M17 | 예외 처리 화면 (404 · 403 · 409 · 4xx · 5xx) | 완료 | `001d65d` | `AdminViewExceptionHandler`, `templates/error/` |
| M18 | ERD 문서화 | 완료 | `928caf4`, `0959196` | `docs/erd*.tsv`, `docs/erd.md` |

## Should Have (권장)

| # | 기능 | 상태 | 커밋 | 주요 경로 |
|---|---|---|---|---|
| S1 | **창고 2D 도면 맵** | 완료 | `eef3f0f` | `/admin/warehouse-map` |
| S1-1 | └ 좌표 기반 자유 배치 평면도 (26×14 격자), 창고 2동 탭 | 완료 | `54a9f34` | `Warehouse`, `posX/posY/posWidth/posHeight` |
| S1-2 | └ 구역 용도 구분 (보관 · 입고대기 · 출고대기 · 검수) | 완료 | `54a9f34` | `BinPurpose` |
| S1-3 | └ 저온(COLD) / 상온 구역 경계 표시 | 완료 | `4ed837d` | `warehouse/map.html` |
| S1-4 | └ 적재율 색상 구분 · 타일 클릭 상세 모달 | 완료 | `eef3f0f` | `WarehouseMapApiController` |
| S1-5 | └ 사용률 산정 정책 확정 (보관 구역만 분모) | 완료 | `fb0667c` | `WarehouseMapSummaryDto` |
| S2 | **제품 이력 추적 뷰어** | 완료 | `311a9a1` | `/admin/traceability` |
| S2-1 | └ 로트번호 검색 → 생애주기 수직 타임라인 | 완료 | `311a9a1` | `TraceabilityService` |
| S2-2 | └ 시점별 잔여 수량 누적 (`MovementType.sign` 활용) | 완료 | `311a9a1` | `TraceEventDto` |
| S2-3 | └ 이력–재고 불일치 경고 | 완료 | `311a9a1` | `TraceabilityDto` |
| S2-4 | └ JSON API (타 화면 팝업용) | 완료 | `311a9a1` | `/api/admin/traceability/lots/{lotId}` |
| S3 | **구역 간 재고 이동 (MOVE)** | 완료 | `a48bb5e` | `/admin/inventory/move` |
| S3-1 | └ 총 재고 불변 (위치만 변경) — `lotQuantity`·`totalStock` 미변경 | 완료 | `a48bb5e` | `InventoryMoveService` |
| S3-2 | └ 출발/도착 구역 검증 (같은 구역·수량 초과·사용중지·적재 한도) | 완료 | `a48bb5e` | `WarehouseBin.canAccept()` 재사용 |
| S3-3 | └ 이력에 출발지+도착지 기록 | 완료 | `a48bb5e` | `StockMovement.fromBin` |
| S3-4 | └ 2D 도면 모달에서 이동 화면 진입 | 완료 | `a48bb5e` | `warehouse-map.js` |
| S3-5 | └ 이력 추적 타임라인에 `A-01 → B-02` 표시 | 완료 | `a48bb5e` | `TraceEventDto.isRelocation()` |

## Epic: 전국 다중 창고 기반 물류망 확장 (P4a · P4c 완료)

WMS 1.0 은 **단일 물류센터 안에 창고 건물 2동(`WH1` · `WH2`)** 을 전제로 만들었다.
이를 **전국 여러 센터(Center)** 구조로 확장한다. 덩치가 커서 4단계로 나눈다.

| Phase | 범위 | 상태 |
|---|---|---|
| **P1** | 기초 공사 — `Warehouse` enum → `Center` 엔티티 승격, 구역(Bin) 매핑 전환 | **완료** `8f7aaf8` |
| **P2** | 검색 및 조회 — 재고·이력 조회에 센터 필터 조건 추가 | **완료** `3e77941` |
| **P3a** | 센터 간 이동 — `TRANSFER_OUT` · `TRANSFER_IN` 신설, `IN_TRANSIT` 가상 구역 | **완료** |
| P3b | 운송 중 상태 관리 — 도착 처리 분리 · 이관 전표 | 보류 (범위 초과) |
| **P4a** | 대시보드 — 센터별 재고 분포 · 실적 시각화 | **완료** |
| **P4c** | 전국 거점 지도 — `Center` 좌표 컬럼 + 2D 도면 양방향 연동 | **완료** |
| P4b | 스마트 주문 할당 — 배송지 → 센터 라우팅 | 보류 (배송지 좌표 · 권역 우선순위 부재) |

### 확정된 정책

| 항목 | 결정 | 근거 |
|---|---|---|
| **`Product.totalStock`** | **전국 합계로 유지** | B2C 쇼핑몰이 이 컬럼을 '판매 가능 수량' 으로 쓰고 있다. 센터별로 쪼개면 연동이 깨진다. 센터별 가용 재고는 **동적 쿼리 · 별도 집계**로 푼다. |
| 재고 3계층 불변식 | 유지 | `totalStock` = Σ`lotQuantity` = Σ`Inventory.quantity`. 센터가 늘어도 성립한다(전국 합계 기준). |
| 센터 간 이동에 `MOVE` 재사용 | **금지** | `MOVE` 는 sign 0(총량 불변)이지만 센터 간에는 한쪽 센터의 재고가 실제로 줄어든다. P3 에서 별도 유형을 만든다. |

### Phase 1 상세

| # | 항목 | 상태 | 커밋 | 비고 |
|---|---|---|---|---|
| P1-1 | `Center` 엔티티 · `CenterRepository` · `CenterDto` 신규 | 완료 | `8f7aaf8` | `domain/Warehouse.java` 파일 삭제 |
| P1-2 | `WarehouseBin` → `Center` `@ManyToOne` 참조 전환 | 완료 | `8f7aaf8` | `warehouse` enum 컬럼 → `centerId` FK (`optional = false`) |
| P1-3 | 조회 쿼리에 `join fetch center` 적용 (N+1 방지) | 완료 | `8f7aaf8` | `locationLabel()` 이 센터명을 쓴다 |
| P1-4 | `data.sql` 전면 수정 (`centers` 시드 2행 + 구역 40행 매핑) | 완료 | `8f7aaf8` | 정합성 4규칙 유지 |
| P1-5 | 2D 도면 · 구역 관리 · 선택 목록 화면의 센터 참조 전환 | 완료 | `8f7aaf8` | 창고 탭 → 센터 탭, 센터 필터, 센터별 `<optgroup>` |
| P1-6 | `WarehouseMapRow` 센터 컴포넌트 제거 | 완료 | `8f7aaf8` | 도면은 센터 단위이므로 행마다 담을 필요가 없다 |
| P1-7 | `WarehouseFacilityDto.forWarehouse` → `forCenter(Long)` | 완료 | `8f7aaf8` | WH1/WH2 배치가 동일해 `switch` 를 걷어냈다 |
| P1-8 | 운영 중인 센터가 없을 때 빈 도면 처리 | 완료 | `8f7aaf8` | `centerId = null` 조회는 전체 센터 구역을 겹쳐 그린다 |
| P1-9 | ERD 문서 갱신 (`centers` 테이블 · 관계) | 완료 | `8f7aaf8` | 실제 스키마와 벌어져 있던 컬럼도 함께 동기화 |

> **Phase 1 은 '구조 전환' 이므로 데이터의 의미는 바꾸지 않는다.**
> 센터 코드(`WH1`/`WH2`)와 이름(제1창고/제2창고)을 그대로 유지해, 화면 결과가
> 이전과 동일한지로 회귀를 판정할 수 있게 한다. 전국 센터 코드 체계 재정의는
> P2 이후에 데이터만 교체하면 된다.

### Phase 2 상세

| # | 항목 | 상태 | 커밋 | 비고 |
|---|---|---|---|---|
| P2-1 | `InventoryRepository.search` 에 `centerId` 조건 추가 | 완료 | `3e77941` | null = 전국. 정렬은 유통기한 1순위 유지(FEFO 확인 화면) |
| P2-2 | 센터별 재고 분포 집계 `findStockByCenter` | 완료 | `3e77941` | **목록 필터와 무관하게** 집계 — 다른 센터에도 재고가 있음을 알려야 한다 |
| P2-3 | 재고 현황 화면에 센터 필터 · 센터 컬럼 · 분포 칩 | 완료 | `3e77941` | 구역 select 은 센터별 `<optgroup>` |
| P2-4 | 조회 결과 합계를 목록 헤더에 표시 (`InventorySearchDto`) | 완료 | `3e77941` | 요약 카드는 전국 기준이라 필터 시 어긋나 보이는 문제 해소 |
| P2-5 | 센터·구역 모순 조건 안내 (`isBinOutsideCenter`) | 완료 | `3e77941` | 0건이 '재고 없음' 인지 '조건 오류' 인지 구분 |
| P2-6 | `TraceEventDto` 에 센터 정보 추가 | 완료 | `3e77941` | 이벤트마다 어느 센터에서 일어난 일인지 |
| P2-7 | 타임라인 센터 표기 3분기 | 완료 | `3e77941` | 센터 내 이동 / 센터 간 이동(경고) / 그 외 |
| P2-8 | `isCenterTransfer()` — 센터 넘는 MOVE 감지 | 완료 | `3e77941` | P3 이전에는 나와서는 안 되는 이력이므로 경고로 노출 |
| P2-9 | 거쳐 간 센터 · 현재 보관 센터 요약 | 완료 | `3e77941` | 회수(recall) 범위 판단 근거 |
| P2-10 | P1 잔여 N+1 수정 (`join fetch b.center`) | 완료 | `3e77941` | `findByLotIdWithBin` · `findWithDetailById` · `findDisposalTargets` |

> **`Product.totalStock` 은 손대지 않았다.** 전국 합계를 유지하고, 센터별 수량은
> `findStockByCenter` 집계 쿼리로 구한다. 비정규화 캐시를 하나 더 두면(`centerStock`)
> 갱신 지점이 늘어 정합성 위험만 커진다.

### Phase 2 에서 의도적으로 미룬 것

| 항목 | 이유 |
|---|---|
| 폐기 화면(`/admin/inventory/disposal`) 센터 필터 | 폐기는 만료 재고를 **전국 단위로 훑는 것이 기본 동작**이다. 센터로 좁히는 요구가 실제로 나오면 `:centerId` 를 추가한다. N+1 만 먼저 고쳤다. |
| 입·출고 이력 목록(`/admin/inventory/movements`) 센터 필터 | 이력 목록은 품목·유형 축으로 보는 화면이다. 센터 축이 필요해지는 시점은 P4(센터별 실적)에 가깝다. |
| 센터 간 이동 **차단** | 이동 화면은 여전히 다른 센터의 구역을 고를 수 있다. 막으려면 `TRANSFER_*` 유형과 운송 중 상태가 함께 있어야 하므로 P3 에서 한 번에 처리한다. P2 에서는 **발생 시 경고로 드러나게만** 했다. |

### Phase 1 에서 의도적으로 미루는 것

| 항목 | 이유 |
|---|---|
| 도면 격자 크기를 센터별 속성으로 이관 | `WarehouseBinForm` 의 `@Max(GRID_COLUMNS)` 가 컴파일 상수를 요구해 폼 검증 방식까지 바꿔야 한다. 센터마다 건물 크기가 다른 것은 사실이지만 P1 범위를 넘는다. |
| 부대시설(출입구·벽·검수실) 테이블 이관 | 센터가 늘면 `centerFacilities` 테이블이 필요하다. P1 에서는 `switch` 를 걷어내 **이관 지점을 `forCenter(Long)` 한 곳으로 좁히는 것**까지만 했다. |
| 센터 간 이동 차단 | 이동 화면의 도착 구역 목록에 **다른 센터의 구역도 나온다.** 센터 간 이동은 총량 불변이 아니므로 P3 에서 `TRANSFER_*` 유형과 함께 구분해 막는다. |

### Phase 3a 상세

설계 결정 근거는 [epic-p3-design.md](epic-p3-design.md) 참고.

| # | 항목 | 상태 | 비고 |
|---|---|---|---|
| P3a-1 | `MovementType.TRANSFER_OUT`(-1) / `TRANSFER_IN`(+1) 신설 | 완료 | 두 건의 합이 0 → 전국 총량 불변 |
| P3a-2 | `BinPurpose.IN_TRANSIT` + `physicalSpace` / `systemManaged` 축 분리 | 완료 | 대기 구역(실제 바닥)과 운송 중(가상)을 구분 |
| P3a-3 | 출발 센터 소속 `IN_TRANSIT` 가상 구역 (센터당 1개, 없으면 자동 생성) | 완료 | `TRANSIT-{centerCode}` |
| P3a-4 | 센터 간 이관 분기 — 같은 센터는 `MOVE`, 다른 센터는 2건 | 완료 | 화면은 이동 하나뿐, 서비스가 판단 |
| P3a-5 | 적재 한도 검증 면제 (물리적 공간이 아니다) | 완료 | 큰 수로 우회하지 않고 검증 자체를 건너뜀 |
| P3a-6 | 가상 구역 노출 차단 (도면 · 선택 목록 · 용도 select) | 완료 | 5개 지점 |
| P3a-7 | 운송 중 구역 직접 이동 거부 | 완료 | 요청 직접 조립 방어 |
| P3a-8 | 정합성 점검에 운송 중 잔류 재고 항목 | 완료 | 감추기만 하면 이상을 알 수 없다 |
| P3a-9 | 이력 타임라인 이관 구간 표기 | 완료 | 출고 구간 / 입고 구간 구분 |
| P3a-10 | `data.sql` 에 `IN_TRANSIT` 시드 2행 | 완료 | binId 41 · 42 |

> **`MOVE` 는 이제 센터 내부 전용이다.** 유효하지 않은 상태를 검증으로 걸러내는 것보다
> 애초에 만들 수 없게 하는 것이 낫다. P2 에서 넣은 `isCenterTransfer()` 경고는
> 그 이전에 남은 이력을 계속 드러내기 위해 유지한다.

### Phase 3a 에서 의도적으로 하지 않은 것

| 항목 | 이유 |
|---|---|
| 이관 전표 엔티티 · 관리 화면 | 포트폴리오 일정 대비 과하다(Over-engineering). 구조가 준비되어 있어 나중에 얹을 수 있다 |
| 실제 운송 중 상태 (도착 처리 분리) | 두 구간이 이미 분리되어 있으므로 두 번째를 나중에 호출하면 된다. 지금은 한 트랜잭션에서 즉시 완료 |
| 운송 리드타임 · 도착 예정일 · 승인 흐름 | 전표가 있어야 의미가 생긴다 |

### Phase 4a 상세

| # | 항목 | 상태 | 비고 |
|---|---|---|---|
| P4a-1 | 시드를 전국 5거점으로 교체 | 완료 | 예산 · 김제 · 의성 · 안성 · 나주 |
| P4a-2 | 센터 특징을 재고 비중으로 반영 (가중 배분) | 완료 | 구역(zone)도 축종 코드(CT/PG/PL/COLD) |
| P4a-3 | 센터별 재고 분포 집계 · 도넛 차트 | 완료 | `/api/admin/center-stock` (STAFF 도 조회) |
| P4a-4 | 센터별 기간 실적 집계 (입고 · 출고 · 폐기 · 이관) | 완료 | 도착 구역 기준 → 이관 방향이 갈린다 |
| P4a-5 | 센터별 유통기한 경보 집계 | 완료 | 안전재고는 전국 기준 유지 (센터별 기준이 없다) |
| P4a-6 | 센터별 축종 구성 집계 | 완료 | 운영 방향이 실제 재고로 지켜지는지 대조 |
| P4a-7 | 센터 카드 + 전국 요약 (재고 최다 · 적재율 최고 · 임박 최다) | 완료 | 적재율 경계는 2D 도면과 공유 |

> **안전재고는 센터별로 나누지 않았다.** `Product.safetyStock` 은 품목 단위 기준값이고
> `totalStock` 은 전국 합계다. "예산 센터의 안전재고 미달" 은 정의되지 않은 개념이므로
> 만들지 않았다. 대시보드의 안전재고 알림은 전국 기준을 그대로 쓴다.

### Phase 4c 상세 — 전국 거점 지도

2D 도면은 창고 한 동의 내부를 보여준다. 전국 5거점으로 넓혔어도 도면만으로는
그 창고가 전국에서 어디인지 드러나지 않아 탭 이름을 읽어야 알 수 있었다.

| # | 항목 | 상태 | 비고 |
|---|---|---|---|
| P4c-1 | `Center` 에 `latitude` · `longitude` 컬럼 추가 (nullable) | 완료 | 좌표는 파생값이 아닌 기준 정보 |
| P4c-2 | 5거점 좌표 시드 | 완료 | 읍 · 면 중심 기준 |
| P4c-3 | 핀 조회 API `GET /api/admin/center-pins` | 완료 | 좌표 + 재고 + 적재율을 한 응답에 (STAFF 도 조회) |
| P4c-4 | Leaflet + OSM 지도 렌더링 | 완료 | API 키 없이 동작 |
| P4c-5 | 도면 탭 → 지도 핀 강조 · 중심 이동 | 완료 | 같은 센터면 페이지 이동 없이 지도만 이동 |
| P4c-6 | 지도 핀 → 도면 전환 | 완료 | 도면은 서버 렌더링이라 `?centerId=` 로 재요청 |
| P4c-7 | 좌표 미등록 센터 수 알림 (`missingCount`) | 완료 | 조용히 빼면 핀 수 차이를 눈치채지 못한다 |

**확정된 정책**

| 항목 | 결정 | 근거 |
|---|---|---|
| 좌표 저장 위치 | **`Center` 엔티티 컬럼** | 좌표는 센터가 가진 속성이다. 프론트엔드 Geocoder 는 API 키 · 호출 한도가 있고 실패 시 핀이 0개가 된다. 기획 주소가 `"고덕면 몽곡리 667 일대"` 처럼 범위라 변환 결과가 호출마다 달라질 수 있다 |
| 좌표 `nullable` | **허용** | 부지 확정 전 센터 등록을 막지 않는다. 좌표가 없으면 지도에서만 빠지고 도면 · 재고 · 이관은 정상 동작한다. 위도만 있는 센터도 누락으로 센다 |
| 지도 라이브러리 | **Leaflet 1.9.4 + OpenStreetMap** | 카카오맵 · 네이버지도는 앱 키 발급 + 도메인 등록 없이는 지도가 아예 뜨지 않는다. 포트폴리오를 받아 실행하는 사람이 빈 화면을 보게 된다. 교체 지점은 `createMap` / `addPin` / `focusPin` 세 함수로 국소화했다 |
| 지도 데이터 전달 | **HTML 이 아니라 별도 `fetch`** | 지도는 보조 정보다. CDN · 타일 서버가 죽어도 2D 도면 본문은 그대로 보여야 한다 |
| 좌표 + 재고를 한 응답에 | 묶어서 내려준다 | 따로 요청하면 두 응답을 화면에서 다시 짝지어야 하고, 한쪽만 실패한 경우의 처리도 늘어난다 |

### Phase 4b 를 여전히 보류하는 이유

P4c 로 **센터 좌표는 확보했다.** 그래도 두 가지가 없다.

| 필요 | 현재 |
|---|---|
| ~~센터 좌표~~ | 확보 (`Center.latitude` · `longitude`) |
| 주문 배송지 좌표 | `Order.shippingAddress` 문자열뿐. 지오코딩 없이는 주소 파싱이 된다 |
| 권역 → 센터 우선순위 | 없다 |

거리만으로 정할 수도 없다. **최단 거리 센터가 그 축종을 취급하지 않을 수 있다** —
나주는 가금 전용이라 소 사료 주문을 받을 수 없다. 거리 + 취급 축종 + 가용 재고를
함께 보는 규칙이 필요하고, 좌표가 있다고 해결되는 문제가 아니다.

### 시드 데이터 밸런싱 (P4c 와 함께 처리)

정합성 규칙을 다 지켜도 **기능을 보여주지 못하는 데이터**가 만들어진다.

| 증상 | 규칙 위반 | 화면에서 보이는 것 |
|---|---|---|
| 최근 7일 입고 +1,660 / 출고 -10 | 없음 | "물류가 멈춘 창고" |
| 활성 보관 구역인데 재고 0 | 없음 | 2D 도면의 영구 빈 칸 |
| 시드 상태에서 이미 포화(80%↑) | 없음 | 재고 이동을 시연할 여유가 없다 |

조치:

| # | 항목 | 결과 |
|---|---|---|
| 1 | `SHIPPED` · `DELIVERED` 주문 10건 전부에 `OUTBOUND` 부여 | 출고 180포대 |
| 2 | 짝이 되는 `INBOUND` 를 **25일 더 오래된 날짜**로 추가 | 기간 집계는 움직이고 최종 재고 배치는 불변 |
| 3 | 로트를 적재율 낮은 구역 순으로 분할 적재 (최소 100포대) | 포화 0곳 · 활성 구역 빈칸 0곳 |
| 4 | 저온 구역을 영양제 취급 센터에만 배치 | 나주(가금 전용)의 영구 빈 구역 제거 |
| 5 | `로트 × 구역` 단위까지 이력 = 재고 일치 | 이력 추적 화면의 정합성 경고가 뜨지 않는다 |

최근 7일 **입고 +310 / 출고 -180 · 폐기 -15** 로, 대시보드 기간 카드가 한쪽으로
쏠리지 않는다.

## Could Have (선택)

우선순위 순. 아직 착수하지 않았다.

| # | 기능 | 상태 | 설명 |
|---|---|---|---|
| C1 | **발주(Purchase Order) 관리** | 미착수 | 입고 처리(M7) 자체는 이미 구현되어 있다. 미구현인 것은 그 **앞단**이다: 공급업체(Supplier) 관리, 발주서 생성·승인, 발주 대비 입고 대조(부분 입고·초과 입고 판정), 매입 단가/금액 집계. `Supplier`·`PurchaseOrder` 엔티티가 없어 신규 도메인 추가가 필요하다. |
| C2 | 재고 실사(Stocktaking) 및 차이 조정 | 미착수 | M15는 장부끼리(`Product.totalStock` vs `ProductLot.lotQuantity` 합계) 비교다. 실물 카운트를 입력해 장부와의 차이를 조정하는 절차는 없다. `MovementType.ADJUST`(sign 0)가 이미 정의되어 있으나 사용처가 없다. |
| C3 | ~~구역 간 재고 이동~~ | **완료** | Should Have 표의 S3 항목으로 이동했다. |
| C4 | 매출/출고 리포트 확장 | 부분 | 대시보드 차트는 최근 7일 고정(`feedflow.dashboard.chart-default-days`)이다. 기간 지정, 축종별·품목별·고객별 집계, CSV 내보내기가 없다. |

## 명시적 제외 범위

| 항목 | 이유 |
|---|---|
| B2C 농가용 쇼핑몰 (상품 목록, 장바구니, 주문/결제) | 별도 모듈 담당. 통합 DB만 공유한다. `Product.imageUrl`·`description`은 쇼핑몰 연동용으로 엔티티에는 두되 관리자 화면에는 노출하지 않는다. |
| 반품(Return) 절차 | 배송 완료(`DELIVERED`) 주문은 취소 대상이 아니다. 실물이 고객에게 있어 창고 재고를 되돌리면 실물과 장부가 어긋난다. |
| 주문 상태 전이 전체 이력 | 취소는 주문당 한 번뿐이라 `Order`의 컬럼으로 충분하다고 판단했다. 모든 상태 전이 기록이 필요해지면 `OrderStatusHistory` 신설을 검토한다. |
| 재고 정합성 드리프트 누적 기록 | H2 in-memory + `ddl-auto=create`라 재시작마다 초기화되어 누적 자체가 불가능하다. |

## 검증 상태

- 단위/통합 테스트 — 테스트 메서드 **242개 선언 / 18개 테스트 클래스**
  (`@ParameterizedTest` 전개 포함 실행 시 약 248건)
- 시드 데이터(`data.sql`) 정합성 — 변경 시 함께 검증해야 한다.
  1. 이력 누적(`SUM(StockMovement × sign)`) = `ProductLot.lotQuantity`
  2. 구역 재고 합계(`SUM(Inventory.quantity)`) = `ProductLot.lotQuantity`
  3. 품목 장부(`Product.totalStock`) = 품목별 로트 수량 합계
  4. 구역별 적재량 ≤ `WarehouseBin.maxCapacity`
  5. **`로트 × 구역` 단위 이력 누적 = 해당 구역의 재고**
     — 4번까지만 지켜도 `MOVE` · `TRANSFER` 의 출발 구역에 입고 기록이 없으면
     구역별로는 어긋난다. 이력 추적 화면의 정합성 경고가 그 조건을 본다.
- 시드 데이터 **표시 품질** — 정합성만으로는 "기능을 보여줄 수 있는 데이터인지" 를 못 잡는다.
  1. 활성 보관 구역에 재고가 0인 곳이 없다 (도면의 영구 빈 칸)
  2. 시드 상태에서 이미 포화(80%↑)인 구역이 없다 (이동 시연 여유)
  3. 최근 7일 입고 · 출고가 모두 0이 아니고 한쪽으로 쏠리지 않는다
  4. 최근 30일 이력이 없는 센터가 없다
  5. 모든 활성 센터에 지도 좌표가 있고, 값이 대한민국 범위 안에 있다
  6. **대기 구역(입고/출고)에 재고가 있다** — 0이면 적재율에서 대기분을 분리한 설계가
     화면에 한 번도 나타나지 않는다. 2D 도면의 `+ 대기 구역 N포대 별도`, 센터 카드의
     `보관 N + 대기 K`, 전국 적재율 안내가 모두 숨는다. H2 인메모리라 손으로 입고해도
     재시작하면 사라지므로, 시드에 심어 두지 않으면 보여줄 방법이 없다.
- 개발 샌드박스는 외부 네트워크가 차단(`INTEGRATIONS_ONLY`)되어 Gradle 의존성을 받을 수 없다.
  **컴파일·테스트 실행은 로컬 환경에서만 가능**하며, 샌드박스에서는 정적 검사로 대체한다 —
  import 누락/미사용, 괄호 균형, enum `switch` 망라성, JPQL `select new` 인자 수 ↔ record
  컴포넌트 수, 테스트 스텁 ↔ 리포지토리 메서드 존재, 템플릿 태그 중첩, 템플릿 `${}` ↔
  컨트롤러 모델 키, 템플릿 프로퍼티 ↔ DTO 게터, JS 가 읽는 JSON 필드 ↔ record 컴포넌트,
  인라인/정적 JS 문법(`node --check`), CSS 괄호 균형.
- **모델 키 ↔ 특정 DTO 프로퍼티 정밀 대조** — 위의 '템플릿 프로퍼티 ↔ DTO 게터' 는
  프로젝트 전체 프로퍼티 이름을 한 집합으로 모아 비교하므로, `${inboundResult.binId}`
  처럼 **객체는 맞고 그 객체에 없는 필드**는 통과한다(`binId` 가 다른 DTO 에 있기 때문).
  실제로 그 실수가 났으므로, 자주 손대는 모델 키는 대상 DTO 를 명시해 1단계 프로퍼티를
  정밀 대조한다.
- **같은 파일 안에서 정의한 메서드 호출의 인자 수** — 테스트 픽스처 헬퍼(`lot(...)`,
  `bin(...)`)는 파일 안에만 있어 IDE 없이 고치다 보면 어긋난다. 오버로드는 허용 인자 수를
  집합으로 모아 비교하고, 가변 인자 메서드는 검사하지 않는다.
- **Mockito `STRICT_STUBS` 안전성** — `@ExtendWith(MockitoExtension.class)` 는 쓰이지 않은
  스텁을 `UnnecessaryStubbingException` 으로 실패시킨다. 리포지토리에 메서드를 추가하고
  기존 테스트에 스텁을 일괄로 붙이면 그 메서드를 쓰지 않는 테스트가 깨진다.
  테스트 메서드별로 "스텁한 리포지토리 메서드" ⊆ "호출한 서비스 메서드 + 손으로 조립한
  협력 객체(`new BinCapacityChecker(...)`)가 실제로 쓰는 집합" 인지 정적으로 대조한다.
